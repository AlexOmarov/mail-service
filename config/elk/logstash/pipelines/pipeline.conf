input {
  tcp {
    port => 5046
  }
}

filter {
  mutate {
    add_tag => [ "logstash_filter_applied" ]
  }
}
output {
  opensearch {
      hosts => ["opensearch:9200"]
      index => "logstash-%{+YYYY.MM.dd}"
      action => "create"
  }
}